# "@airdev/next": "managed"

name: NextauthAccount
# internal for now since we haven't build support for unlinking sso
internal: true
# api:
#   methods:
#     getMany:
prisma:
  internalFields:
    - id
    # - userId
    # - type
    # - provider
    - providerAccountId
    - refresh_token
    - access_token
    - expires_at
    - token_type
    # - scope
    - id_token
    - session_state
